#include "monty.h"

/**
 *nop - doesn’t do anything.
 *@head: ptr to ptr.
 *@line_number: integer num for line.
 *
 **/
void nop(stack_t **head, unsigned int line_number)
{
	(void)head;
	(void)line_number;
}
